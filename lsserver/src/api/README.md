# REST API Reference

This is an API. It is also a WIP.

When running the server, the API reference can be found at [http://{host}:{port}/api-docs/swagger-ui/](http://127.0.0.1:8080/api-docs/swagger-ui/).

The REST API is slow and websocket should probably be preferred, but nonetheless this exists.
